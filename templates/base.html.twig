<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="https://fonts.googleapis.com/css2?family=Kurale&display=swap" rel="stylesheet"/>
        <link rel="stylesheet" href="https://bootswatch.com/5/minty/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <link rel="stylesheet" href="/build/app.css">
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
        {% endblock %}
    </head>
    <body>
    <!---------------------------NAVBAR----------------------->
    <nav class="container navpc">
        <ul class="navbar d-flex p-0 align-items-center justify-content-around m-0">
            <li class="logonavbar">
                <a href="#"><img src="/img/LaNîmesalerie-logos_transparent.png" alt=""/></a>
            </li>
            <!------------------------------SEARCHBAR--------------------------->
            <form class="searchSite">
                <li class="searchBar d-flex align-items-center">
                    <button class="border-0 bg-transparent" type="submit">
                        <i class="fas fa-search"></i>
                    </button>
                    <input class="border-0" type="search" id="recherchesite" name="recherche" placeholder="Que recherchez-vous ? "/>
                </li>
            </form>
            <!-----------------------SIGNIN----------------------->
            <li class="signIn d-flex align-items-center p-0">
                <a href="{{ path('signin') }}"><i class="far fa-user"></i></a>
                <div>
                    {% if app.user %}
                        <div class="btn-group" role="group" aria-label="Button group with nested dropdown">
                            <button type="button" class="btn btn-light">{{ app.user.username }}</button>
                            <div class="btn-group" role="group">
                                <button id="btnGroupDrop1" type="button" class="btn btn-light dropdown-toggle" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false"></button>
                                <div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
                                    <a class="dropdown-item" href="{{ path('user_profile')}}">Mon profil</a>
                                    <a class="dropdown-item" href="{{ path('signout') }}">Déconnexion</a>
                                </div>
                            </div>
                        </div>
                        {#                        <p class="m-0 fw-bolder">{{ app.user.username }}</p>#}
                    {% else %}
                        <p class="m-0 fw-bolder">Mon compte</p>
                        <p class="m-0">Se connecter</p>
                    {% endif %}
                </div>
            </li>
            <!-------------------------BASKET----------------------->
            <li class="basket d-flex align-items-center">
                <a href=""><i class="fas fa-shopping-basket"></i></a>
                <p class="m-0">Mon panier</p>
            </li>
            <!-------------------------FIND US----------------------->
            <li class="findUs d-flex align-items-center">
                <a href="#"><i class="fas fa-map-marker-alt"></i></a>
                <p class="m-0">Nous trouver</p>
            </li>
        </ul>
    </nav>
    <div class="container">
        {% block body %}{% endblock %}
    </div>

    </body>
</html>
